/**
 * App litterals constants
 */

interface AppLitterals {
	uploadOptions: any;
}

export const AppLitteralsConfig: AppLitterals = {
	uploadOptions: '.jpg,.jpeg,.png,.JPG,.JPEG,.pdf,.PDF,.PNG,.BAT,.bat,.BMP,.bmp,.CSV,.csv,.CVS,.cvs,.doc,.docx,.DOC,.DOCX,.MAC,.mac,.MAP,.map,.PPT,.ppt,.PPTX,.pptx,.PSD,.psd,.PSP,.psp,.QXD,.qxd,.txt,.TXT,.RTF,.rtf,.TAR,.tar,.TIF,.tif,.XLS,.xls,.XLSX,.xlsx,.zip,.ZIP,.rar,.RAR,image/*'
}
